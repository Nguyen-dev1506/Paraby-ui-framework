# Claude Code — Project Instructions

This project's agent rules live in **[`AGENTS.md`](AGENTS.md)** — read it
before making any change. It applies to Claude Code exactly the same as any
other coding agent; the rules are kept in one file on purpose so they can't
drift out of sync between tools.

For DSL syntax specifics when generating or editing `.pui`/`.py` code, see
**[`SYNTAX.md`](SYNTAX.md)**.
